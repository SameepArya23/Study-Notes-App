import { Fragment } from "react";
import NotesInfo from "../notes-info/notes-info.component";

const Notes = () => {
  // const { notes } = useContext(UserDataContext);
  return (
    <Fragment>
      <NotesInfo />
    </Fragment>
  );
};
export default Notes;
